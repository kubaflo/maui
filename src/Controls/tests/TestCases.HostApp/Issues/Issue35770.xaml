<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Maui.Controls.Sample.Issues.Issue35770">
    <Grid Padding="16"
          RowDefinitions="Auto,Auto,Auto,*"
          RowSpacing="12">
        <Label Text="Nested CollectionView scrolling"
               FontSize="20"
               FontAttributes="Bold" />
        <Label Grid.Row="1"
               Text="Drag Row 1 Item 1 upward, then check whether the inner collection moved." />
        <HorizontalStackLayout Grid.Row="2"
                               Spacing="12">
            <Button AutomationId="CheckNestedScroll"
                    Text="Check nested scroll"
                    Clicked="OnCheckClicked" />
            <Label x:Name="ResultLabel"
                   AutomationId="NestedScrollResult"
                   Text="0|-1|0|-1|-1|-1|loading"
                   VerticalOptions="Center"
                   FontAttributes="Bold" />
        </HorizontalStackLayout>
        <CollectionView x:Name="OuterCollectionView"
                        Grid.Row="3">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Padding="0,8"
                                         Spacing="6">
                        <Label Text="{Binding Title}"
                               FontAttributes="Bold" />
                        <CollectionView HeightRequest="220"
                                        ItemsSource="{Binding Items}"
                                        SizeChanged="OnInnerCollectionSizeChanged"
                                        Scrolled="OnInnerCollectionScrolled">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Vertical"
                                                   ItemSpacing="8" />
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Border HeightRequest="64"
                                            Padding="12">
                                        <Label Text="{Binding .}"
                                               VerticalOptions="Center" />
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
