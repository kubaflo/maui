<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Maui.Controls.Sample.MainPage"
             xmlns:local="clr-namespace:Maui.Controls.Sample"
             BackgroundColor="White">
    
    <VerticalStackLayout Padding="20" Spacing="20">
        <Label Text="MediaPicker Test"
               FontSize="24"
               FontAttributes="Bold"
               HorizontalOptions="Center"/>
        
        <Label Text="Test for Issue #32649: PickPhotosAsync returning null"
               FontSize="14"
               TextColor="Gray"
               HorizontalOptions="Center"/>
        
        <Button x:Name="PickPhotosButton"
                Text="Pick Photos"
                Clicked="OnPickPhotosClicked"
                BackgroundColor="Blue"
                TextColor="White"/>
        
        <Button x:Name="PickVideosButton"
                Text="Pick Videos"
                Clicked="OnPickVideosClicked"
                BackgroundColor="Green"
                TextColor="White"/>
        
        <Label x:Name="ResultLabel"
               Text="Result will appear here"
               FontSize="16"
               TextColor="Black"
               HorizontalOptions="Center"
               VerticalOptions="Start"/>
        
        <Label x:Name="InstructionsLabel"
               Text="Instructions: Tap a button, then CANCEL the picker (swipe down or tap outside). The result should be an empty list, NOT null."
               FontSize="12"
               TextColor="DarkRed"
               HorizontalOptions="Center"
               Margin="0,20,0,0"/>
    </VerticalStackLayout>
</ContentPage>